Real Element
============

     AlgebraElement = require 'AlgebraElement'

     class RealElement extends AlgebraElement
       constructor: (num = 0) ->
         super.data num

       @add: ->
       @addition = @add
       
     exports = RealElement

