
/**
 * Compute index of multi dim array
 *
 * @param {Array} indices
 * @param {Array} dimensions
 *
 * @return {Number} index
 */

function multiDimensionalArrayIndex(indices, dimensions) {
  var index = 0

  return index
}

module.exports = multiDimensionalArrayIndex

