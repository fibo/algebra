
var should = require('should')

/*
var constructCayleyDicksonAlgebra = require('../src/constructCayleyDicksonAlgebra')

// Create real field
var algebraGroup = require('algebra-group'),
    algebraRing  = require('algebra-ring')

function contains (a) {
  // NaN, Infinity and -Infinity are not allowed
  return (typeof a === 'number' && isFinite(a))
}

function equality (a, b) { return a === b }

function addition (a, b) { return a + b }

function negation (a) { return -a }

var realGroup = algebraGroup({
  identity: 0,
  contains: contains,
  addition: addition,
  equality: equality,
  negation: negation
})

function multiplication (a, b) { return a * b }

function inversion (a) { return 1 / a }

var R = algebraRing(realGroup, 1, multiplication, inversion)

var C, // complex
    H, // quaternion
    O  // octonion

describe('constructCayleyDicksonAlgebra', function () {
  it('builds a sequence of algebras', function () {
    C = constructCayleyDicksonAlgebra(R, 1)
    H = constructCayleyDicksonAlgebra(R, 2)
    O = constructCayleyDicksonAlgebra(R, 3)
  })

  it('creates identities', function () {
    should.deepEqual(C.one, [1, 0])
  })
})

*/

