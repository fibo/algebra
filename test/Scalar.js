var Scalar = require('algebra').Scalar
var realField = require('../src/realField')

describe('Scalar', () => {
  it('checks n is 1, 2, 4 or 8'/*, () => {
    ;(() => {
      Scalar(realField, 3)
    }).should.throw()
  }*/)
})
